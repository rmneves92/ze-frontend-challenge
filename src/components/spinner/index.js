import React from 'react';
import { SpinnerContainer, SpinnerElement } from './styles';

const Spinner = (props) => {
  return (
    <SpinnerContainer>
      <SpinnerElement />
    </SpinnerContainer>
  );
};

export default Spinner;
